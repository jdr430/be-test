# Mock mode is ON. The UI returns canned data from src/lib/mock.ts and
# does NOT talk to any backend. Set NEXT_PUBLIC_MOCK=false (or delete the
# line) once your Go + Node services are running. See README §2.1.
NEXT_PUBLIC_MOCK=true

NEXT_PUBLIC_WALLET_API=http://localhost:8081
NEXT_PUBLIC_PROFILE_API=http://localhost:8082
